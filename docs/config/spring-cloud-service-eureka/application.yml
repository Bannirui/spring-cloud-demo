eureka:
  instance:
    hostname: localhost
  client:
    # 使用服务端模式
    registerWithEureka: false
    fetchRegistry: false
    # 配置自己的eureka的地址
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/